import { OrderDataPipe } from './order-data.pipe';

describe('OrderDataPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDataPipe();
    expect(pipe).toBeTruthy();
  });
});
